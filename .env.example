# ============================================================
# 環境變數範本
# ------------------------------------------------------------
# 用途：
#   1. 本地開發 → 複製本檔為 `.env`，填入真實值
#   2. Vercel 部署 → 到 Vercel 專案 Settings → Environment Variables
#      逐一把下列變數加入（Production / Preview 環境都要加）
#
# 注意：本檔案不含真實值，可安全提交到 git；
#       真正的 `.env` 已被 .gitignore 忽略，絕不會進 git。
# ============================================================

# --- 資料庫（PostgreSQL）---
# 本地：你的本機 PostgreSQL 連線字串
# Vercel：你的「雲端 PostgreSQL」連線字串（如 Neon / Supabase / Railway / Render）
#   ※ Vercel 是 serverless，無法連本機資料庫，必須用雲端資料庫
#   ※ 若是 Neon / 雲端 Postgres，連線字串通常需要 ?sslmode=require
DATABASE_URL="postgresql://使用者:密碼@主機:5432/資料庫名?sslmode=require"

# --- Email（Gmail SMTP，用於寄送 OTP 登入驗證碼）---
# SMTP_USER：Gmail 地址
# SMTP_PASS：Gmail「應用程式密碼」（16 位），不是登入密碼
#   申請方式：Google 帳號 → 帳戶安全性 → 兩步驟驗證 → 應用程式密碼
SMTP_USER="your-gmail@gmail.com"
SMTP_PASS="your-16-digit-app-password"

# --- Session（JWT 密鑰）---
# 用 jose 以 HS256 簽署 session cookie，必須設定一個夠長的隨機字串
# 產生方式（任選其一）：
#   PowerShell： -Length 48 | ForEach-Object { $_ * 2 } | Select-Object -First 1
#   或 Node：    node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# ⚠️ 生產環境（Vercel）務必設定，否則會回退到不安全的預設值
SESSION_SECRET="請換成一個夠長的隨機字串_at_least_32_chars"
